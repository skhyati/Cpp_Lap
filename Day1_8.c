#include <stdio.h>
int main()
{
    int number = 10; // variable decln + init 
    double basic_salary = 20000.0; // variable decln + init 
    char letter = 'A'; // variable decln + init 
    int roll_number; // variable declaration 
    roll_number = 20; // assignment 
    double price = 200.0;// variable decln + init 
    price = 300.0; // assignment 
    return 0;
}
